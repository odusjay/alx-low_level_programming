#include <stdio.h>
/**
 * main-serve as the entry point for the program
 *
 * Return:returns zero if no errors otherwise returns non-xero value.
 */
int main(void)
{
	printf("with proper grammar,but the outcome is a piece of art,\n");
	return (0);
}
